{% extends "base.html.twig" %}

{% block stylesheets %}<link rel="stylesheet" href="{{ asset('css/books.css') }}">{% endblock stylesheets %}

{% block main %}
    <main>
        {#      <a class="link" href="{{ path('admin_session_create') }}">Créer un session</a> #}
        <section class="section">
            {#          {% for session in sessions %} #}
                <div class="card">
                    <div class="card-text">
                        {#                     <h2>{{ session.title }}</h2> #}
                        {#                    <img src="{{ asset('uploads/cover/'~session.coverFilename) }}" alt=""> #}
                        {#                    <h3>{{ session.content}}</h3> #}
                        {#                   {% if session.skatepark %} #}
                        {#                      <h3>{{ session.skatepark.town }} {{ session.skatepark.area }}</h3> #}
                        {% endif %}
                    </div>
                </div>
            {% endfor %}
        </section>
    </main>
{% endblock main%}